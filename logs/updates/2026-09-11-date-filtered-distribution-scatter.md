# 2026-09-11 日期过滤散点图恢复

## 本次目标

- 菜单分析、全局埋点和用户行为恢复为每项数据一个圆点的分布散点图。
- 三个散点图只展示顶部所选日期范围内的数据。

## 修改范围

- `src/app/menus/page.tsx`：菜单分布恢复单点散点。
- `src/app/global-clicks/page.tsx`：点击元素分布恢复单点散点。
- `src/components/users/user-distribution-scatter.tsx`：用户分布恢复单点散点。
- `src/components/data-chart/distribution-scatter.tsx`：移除聚合气泡模式，只保留统一的单点散点入口。
- `src/app/globals.css`：移除聚合气泡专用样式。
- 删除无引用的聚合气泡组件和计算工具。

## 数据链路

- 菜单和全局点击查询使用顶部日期范围过滤 `event_date`。
- 用户行为查询使用顶部日期范围过滤 `occurred_at`。
- 日期切换后，页面列表、趋势图、指标和分布散点使用同一时间范围重新查询。

## 影响范围

- 仅影响三个页面底部的分布图表达，不改变数据库、Worker、事件采集或统计字段含义。

## 自检

- TypeScript 类型检查通过。
- Next.js 生产构建通过。
- 菜单分析使用真实本机数据验证：2026-09-03 至 2026-09-09 显示 17 个散点，切换为 2026-09-09 单日后显示 9 个散点。
- 全局埋点使用真实本机数据验证：2026-09-03 至 2026-09-09 显示 153 个散点，切换为 2026-09-09 单日后显示 28 个散点。
- 两组结果证明日期切换会重新生成散点，而不是只改变页面日期文案。
- 用户行为页待发布后使用 Cloudflare D1 实际数据复核。

## 遗留问题

- 暂无。
