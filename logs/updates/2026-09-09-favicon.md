# 2026-09-09 面板浏览器图标

## 本次目标

使用用户提供的 `logo-seo.png` 作为 TKF Signal 数据分析面板的浏览器标签图标，消除页面请求缺少 `favicon.ico` 的提示。

## 修改范围

- Next.js 应用图标资源；
- 根布局元数据；
- 线上 Vercel 部署。

## 完成内容

- 将用户提供的 Logo 复制为 `src/app/icon.png`。
- 在 `src/app/layout.tsx` 中声明浏览器图标、快捷方式图标和 Apple 图标。
- 未修改左侧面板 Logo，也未修改 Shopify 主题。

## 自检结果

- 源文件与项目图标 SHA-256 一致，资源大小 480,528 字节。
- 主项目 TypeScript 检查和生产构建通过。
- Vercel 生产部署成功，线上 `/icon.png` 返回 HTTP 200。
- 真实浏览器确认已请求 `/icon.png`，控制台错误数为 0。

## 遗留问题

- 无。
