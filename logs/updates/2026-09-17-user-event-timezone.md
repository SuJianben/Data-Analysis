# 2026-09-17 用户行为时间与 Shopify 对齐

## 本次目标

- 修正用户行为详情中直接显示 UTC 时间的问题，使其与 Shopify 后台当前使用的中国时间一致。

## 修改范围

- `src/utils/date-time.ts`
- `src/components/users/user-event-table.tsx`
- `src/components/users/user-table.tsx`

## 新增与调整

- 新增统一时间格式工具，所有用户行为时间明确指定 `Asia/Shanghai`。
- 行为详情保留完整年月日和秒，便于与 Shopify 订单时间核对。
- 用户列表改为复用同一工具，现有显示格式不变。

## 影响范围

- 仅改变 TKF Signal 用户行为页的时间显示；不改动原始事件时间、数据库记录、GA4 或 Shopify 像素。

## 自检

- `npm run typecheck`：通过。
- `npm run build`：Next.js 16.3.4 生产构建通过。
- 时间换算回归：`2026-09-16T19:20:34.000Z` 输出为 `2026/9/17 03:20:34`。
- 真实页面链路：使用独立临时数据库启动生产服务，访问用户行为详情页返回 HTTP 200，页面同时渲染 `purchase` 与 `2026/9/17 03:20:34`。

## 遗留问题

- 本次不调整购买事件的金额、件数和商品名称展示。
- 当前工作区包含先前未提交的多站点埋点改动，本次未直接发布，避免将无关改动一并部署。
