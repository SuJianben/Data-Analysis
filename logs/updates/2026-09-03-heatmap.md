# 2026年09月03日更新日志：页面点击热力图

## 本次目标

为 TKF 站点增加全站通用点击采集，并在本地面板按页面路径展示真实点击热点。

## 新增内容

- Shopify 未发布主题副本 145289216084 新增 assets/tkf-heatmap.js
- layout/theme.liquid 在 </body> 前加载热力采集脚本
- SQLite 新增 heatmap_click_metrics 表，独立保存热力数据
- GA4 同步增加 page_heatmap_click 查询；热力自定义维度未注册时不影响菜单和站点指标同步
- 本地新增 /heatmap 页面和 /api/heatmap 接口

## 数据范围

采集字段为页面路径、20×20 网格坐标、页面区域、元素类型、滚动区间和设备类别；不读取输入框内容、用户文本、订单信息或带参数 URL。

## 自检

- Shopify 主题资产返回 200，脚本包含 page_heatmap_click
- 未发布主题预览返回 200，页面 HTML 已包含脚本引用
- npm run typecheck 通过
- npm run build 通过
- 本地 /heatmap 和 /api/heatmap 返回 200；当前无热力行是因为尚未产生并同步该事件

## 遗留问题

- GA4 需要注册 heatmap_cell、element_group、page_section 三个事件级自定义维度后，API 才能读取坐标聚合数据。
- 截图叠加需要先有真实热力行；当前页面先展示坐标网格，不生成虚假的截图热点。
- Shopify 主主题未发布本次改动，待预览确认后再决定是否发布。
