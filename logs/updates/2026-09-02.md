# 2026-09-02 更新日志

## 本次目标

创建仅在本机运行的数据同步、菜单报表与 AI 分析软件，先打通可验证的完整数据链路。

## 修改范围

- 新建 Next.js + TypeScript 本地全栈应用
- 新建 SQLite 数据库与仓储层
- 新建 GA4、Clarity、OpenAI-compatible AI 连接器
- 新建标准化数据导入接口
- 新建概览、菜单分析、数据源、AI 分析页面

## 新增内容

- `src/app`：页面与本地 API 路由
- `src/components`：应用外壳和各业务页面组件
- `src/features/analysis`：本地规则分析
- `src/services/connectors`：外部服务连接器
- `src/services/database`：数据库初始化与数据访问
- `src/types`：统一数据类型
- `data/analytics.db`：运行后生成的本地数据库

## 调整内容

- GA4 同步同时拉取菜单点击与主要业务事件
- Clarity 数据作为可追溯快照保存
- AI 未配置密钥时自动采用本地规则分析
- AI 上下文包含菜单、业务事件和最近一次 Clarity 快照；大快照会限制长度
- 密钥只在本机后端请求过程中使用，不持久化

## 影响范围

当前为独立新项目，不修改 TKF Shopify 主题、GA4 属性或 Clarity 项目配置。

## 自检

- TypeScript 类型检查：通过
- Next.js 生产构建：通过
- 健康检查接口：通过
- 演示数据导入：通过，共308行
- SQLite 写入与汇总查询：通过
- 本地分析生成：通过
- 菜单文本与设备筛选：通过
- 桌面与390px手机布局：通过
- GA4 缺少 Token 的错误处理：通过

## 遗留问题

- GA4 真实外部同步已通过；Clarity 仍需配置新的本机长期 Token。

## AI 接口补充验证

- 已配置本机 OpenAI-compatible API 环境变量，密钥仅保存在被 Git 忽略的 `.env.local`
- 已通过服务商 `/models` 核对并采用 `gpt-5.6-sol`
- 已完成真实 AI 分析请求，成功返回标题、摘要、3条发现与4条行动建议
- AI 结果已写入本机分析历史，密钥未写入数据库或日志
- 修复第三方 AI 将摘要返回为对象时导致分析页面500的问题
- 新增 AI 输出规范化与旧历史记录展示兜底
- 已验证概览到 AI 分析的真实点击链路、旧记录兼容和新分析生成
- 新增 GA4 服务账号长期认证，支持自动生成短期 Access Token
- 新增 GA4 与 Clarity 固定凭证状态显示
- 新增本机 `secrets/` 凭证目录并确保 JSON 密钥不进入 Git
- 新增 GA4 OAuth Refresh Token 长期认证，同步前自动换取短期 Access Token
- 拆分 `ga4-auth` 认证模块与 GA4 报表连接器，保留服务账号和临时 Token 备用链路
- OAuth 三项凭据已写入本机 `.env.local`，未写入代码、数据库或日志
- 真实自检：不传临时 Access Token 的 API 同步成功，写入 27 行；数据源页面按钮同步成功，写入 50 行
- 真实自检：数源页显示“OAuth 已固定”，菜单报表已显示 GA4 一级/二级菜单、行为、设备和点击目标
- 最终 TypeScript 检查与 Next.js 生产构建通过，OAuth 三项凭据均未出现在客户端构建文件中
